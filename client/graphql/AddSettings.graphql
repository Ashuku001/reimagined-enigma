mutation addSetting($setting: SettingInput!) {
  addSetting(setting: $setting) {
    __typename
    callBack_url
    access_token
    app_id
    app_secret
    phone_number_id
    business_account_id
    access_token
    api_version
    webhook_verification_token
    phone_number
  }
}
