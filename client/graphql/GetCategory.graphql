query GetCategory($categoryId: Int!){
    category(categoryId: $categoryId){
        __typename
        id
        name
        billboard{
            __typename
            id
            label
            imageUrl
        }
    }
}